[build]
  base    = "client" # Make sure "client" is the correct directory
  command = "npm run build"
  publish = ".next"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200
